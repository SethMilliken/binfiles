# Create a ready-to-go screen session
#
source $HOME/.screenrc

chdir $HOME
screen -t  '~'
stuff "ssh-add && git in; git out; git st"

chdir $HOME/.vim
screen -t .vim bash
stuff "git in; git out; git st"

chdir $HOME/bin
screen -t bin bash
stuff "git in; git out; git st"

chdir $HOME/sandbox/personal/
screen -t personal bash
stuff "git in; git out; git st"

chdir $HOME/.pentadactyl
screen -t pentadactyl  bash
stuff "git in; git out; git st"

chdir $HOME/sandbox/work
screen -t work bash

chdir $HOME/staging/vim
screen -t vim-test  bash

chdir $HOME/sandbox/code
screen -t code bash

chdir $HOME
screen -t brew bash
stuff "brew update"

chdir $HOME/bin
screen -t firefox  bash
stuff "v ff"

chdir $HOME/bin
screen -t screen  bash
stuff "vim samsara.screen"

chdir $HOME/bin
screen -t synergy  bash
stuff "less +F ~/Desktop/synergy.log"

chdir $HOME

# vim: ft=screen
